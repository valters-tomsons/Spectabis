<Page x:Class="Spectabis_WPF.Views.GameDiscovery"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Spectabis_WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Medium"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
      Title="GameDiscovery">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Focusable="False" Margin="0,0,0,50" BorderThickness="0">
                <ListView x:Name="GameListView" Focusable="False"/>
            </ScrollViewer>

        <StackPanel Orientation="Horizontal" Margin="20,5" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Margin="15,0" Content="Add Selected" Click="AddSelected_Click" />
            <Button Margin="15,0" Content="Back to Library" Click="Back_Click"/>
        </StackPanel>
    </Grid>
</Page>
